{
	"name": "PCM Auto Decoder (Debian Trixie)",
	"dockerFile": "Dockerfile",
	"postCreateCommand": "rustc --version && cargo --version && pkg-config --modversion libavcodec libpipewire-0.3",
	"customizations": {
		"jetbrains": {
			"backend": "RustRover"
		}
	},
	"remoteUser": "vscode"
}
